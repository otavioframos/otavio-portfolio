# Portfolio direction
Industrial editorial: expressive condensed typography, graphite surfaces, vermilion accents, precise project labels, and large original product visuals.
DFII: impact 4 + fit 5 + feasibility 4 + performance 4 - consistency risk 2 = 15.
Audience: recruiters and design hiring managers evaluating product ownership, design craft, and practical AI experience.
Anchor: oversized condensed thesis paired with a flowing monochrome field annotated with tracked regions, pale green contours, measured coordinates, and orange trails; numbered full-width project presentations.
Typography: Barlow Condensed for display; Manrope for readable copy; IBM Plex Mono for project metadata.
Motion: procedural flow field rendered with Canvas 2D, using GSAP quickTo for pointer-driven distortion. Connected-component detection measures bright regions; nearest-centroid association preserves IDs across frames. Bounding boxes, coordinates, contours, connecting lines, and trails derive from those measurements. This is a generative tracking study, not camera/video analysis or a physics fluid solver. Rendering is capped at 24 fps with a 112×124 scalar field and 1.5 pixel ratio for crisp annotations. It pauses offscreen, in hidden tabs, with reduced motion, and via its pause button. Touch keeps native scrolling; an inline SVG supplies a static fallback.
Refinement: a two-column desktop hero, stacked mobile tracking study, tighter editorial labels, clearer project/link feedback, and a creative-coding interest paragraph in both languages. Existing studies and case narratives are retained.
Validation: production build, TypeScript, and targeted new-component lint passed. Local production HTTP checks returned 200 with expected content for both homepages and English/Portuguese case routes; both homepages retain Lorenna Mello's course. Existing raw-image lint advisories in the original portfolio component remain. Browser interaction and visual QA have not been run.
Content: based on the user's statements, LinkedIn export, existing Notion case studies, and public MindYoung site. No internal revenue, invented test results, or unconfirmed employment dates.
Assets: original public portfolio and product images, copied locally. Content Radar images are design artifacts with illustrative cards; Avela imagery is a design exploration, not verified testimonial evidence.
Draft remains private and noindex. No social-preview image requested or created.

Tracking revision validation: production build, TypeScript, targeted lint; synthetic checks of region bounds, identity continuity, unique IDs, and finite field samples. Browser visual/interaction QA has not been run. User-supplied tracking images informed the style; they are not used as site assets.

## Nature / computation revision
The user's own draft now anchors the art direction: Instrument Serif display type, Pixelify Sans accents, Manrope body text, a blue-hour poppy meadow, and restrained glass surfaces. DFII: 5 impact + 5 fit + 4 feasibility + 4 performance - 3 consistency risk = 15. Hero leads with Otávio's name, original design philosophy, and explicit A3Lab product-design scope. The user's personal notes return with concise copy. Three compact cards lead into the existing full case studies; all education remains.
One original generated meadow image is optimized to a 392 KB JPEG. Its red/bright flower regions are measured once from image pixels, and boxes follow the same gentle displacement applied to the image. This is an animated still-image study, not live-video tracking. Animation pauses offscreen, in hidden tabs, for reduced motion, and via the visible pause control. No camera, uploaded reference screenshots, or unverified brand logos are used.
Validation: TypeScript and production build passed. Local home route returned HTTP 200. Flower-region detection and desktop/mobile cover-coordinate checks passed. No browser visual QA was requested or performed.
